# orchestrator

Natural-language robot instructions are compiled here.

LLM access goes through `openai_llm` only:

```python
from openai_llm import OpenAILLM
```

See [`openai_llm/README.md`](openai_llm/README.md) for key setup, install, and tool-calling examples.
